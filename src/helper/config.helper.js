module.exports = (cfg) => {
    // default size
    cfg.size = cfg.size || 300;

    // default colors
    cfg.bgColor = cfg.bgColor || '#eee';
    cfg.color = cfg.color || '#222';

    // font style
    cfg.fontFamily = cfg.fontFamily || 'Calibri';
    cfg.fontSize = cfg.fontSize || 100;
};